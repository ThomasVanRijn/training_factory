{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-warning d-inline">{{ button_label|default('Aanmaken') }}</button>
    <a class="btn btn-outline-success d-inline" href="{{ path('admin_instructeur_lijst') }}">back to list</a>
{{ form_end(form) }}
