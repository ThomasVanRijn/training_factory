{{ form_start(form) }}
    {{ form_widget(form) }}
    <a class="btn btn-outline-success d-inline" href="{{ path('lesson_index') }}">back to list</a>
    <button class="btn btn-outline-warning d-inline">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
